// Update this once your Render backend is deployed.
// Keeping it in one place means we only edit it here, not scattered across components.
export const API_BASE_URL = 'https://YOUR-APP-NAME.onrender.com';
